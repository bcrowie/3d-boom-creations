import "../../styles/Pages/About.scss"

const About: React.FC = () => {
    return (
        <div className="about">
            <p>This is the about page.</p>
        </div>
    );
};

export default About;
